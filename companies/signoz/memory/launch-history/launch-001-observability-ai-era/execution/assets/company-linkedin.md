# Company LinkedIn — Launch Post

AI is changing observability in two directions at once.

Agents are becoming users of production telemetry. At the same time, LLM and agent workloads are creating larger traces, new signals, and materially more telemetry.

So observability has to evolve for both.

Today we’re bringing four SigNoz capabilities together under one direction: **observability for the AI era — agent-native and AI-scale.**

- **SigNoz MCP** brings logs, metrics, traces, and alerts into MCP-compatible workflows such as Claude Code, Cursor, Codex, and custom agents.
- **Noz** lets engineers work with telemetry in natural language inside SigNoz.
- **Agent-native dashboards** use a strict, validated schema designed to make dashboard creation more dependable for both agents and humans.
- **AI-scale trace improvements** make it easier to investigate very large traces, including LLM-specific attributes and workflows.

One customer, Kernel, already uses an internal agent during incidents to query SigNoz through MCP and return analysis into Slack. **[CUSTOMER PROOF — publish only after approval of exact wording.]**

The important shift isn’t “observability with an AI assistant.”

It’s an observability platform where humans and agents can work over the same OpenTelemetry-native telemetry foundation — while humans stay in control.

**CTA:** [READ THE LAUNCH / TRY SIGNOZ MCP — FINAL URL REQUIRED]
