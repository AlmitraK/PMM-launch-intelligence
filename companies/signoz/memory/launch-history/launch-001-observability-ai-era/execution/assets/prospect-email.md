# Prospect Email

**Subject:** What observability looks like when agents enter the workflow

Hi {{ customer.first_name | default: "there" }},

AI is changing observability in two ways at once.

Developers are increasingly working through coding and operational agents. Meanwhile, AI workloads generate larger traces and new telemetry across model calls, tools, databases, and the rest of the application stack.

That creates a different requirement for the observability platform.

With SigNoz:

- **MCP** can expose logs, metrics, traces, and alerts to MCP-compatible agents.
- **Noz** lets engineers investigate telemetry in natural language inside the product.
- **Agent-native dashboards** use a validated structure designed for dependable agent and human workflows.
- **AI-scale trace improvements** help engineers work with very large traces and LLM-specific telemetry.

The idea is not to create a separate “AI observability” silo.

It’s to let humans and agents operate over the same OpenTelemetry-native telemetry foundation.

**Read how SigNoz is building observability for the AI era → [FINAL TRACKED URL REQUIRED]**

If you want to try the MCP workflow directly: **[FINAL MCP URL REQUIRED]**
